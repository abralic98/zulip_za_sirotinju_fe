query getMessagesByRoomId($roomId: ID) {
  getMessagesByRoomId(roomId: $roomId) {
    account {
      username
      id
    }
    text
    id
    insertedAt
  }
}

query getRooms {
  getRooms {
    id
    name
  }
}

query getAccounts{
  getAccounts{
    username
    status
    id
  }
}